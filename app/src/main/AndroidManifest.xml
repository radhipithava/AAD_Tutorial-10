<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tutorial10_rp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tutorial10_RP">
        <activity android:name=".Welcome"></activity>
        <activity android:name=".Splash" />
        <activity android:name=".Signup" />
        <activity android:name=".MyUtil" />
        <activity android:name=".MyDatabaseHelper" />
        <activity android:name=".FileManagement" />
        <activity android:name=".Display" />
        <activity android:name=".CustomAdapter" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>